# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Transient pipeline output (Story 5/7) — always regenerated by a run and
# promoted into src/data/v04/seed/ + public/data/; never a shipped artifact.
src/data/v04/candidate/
scripts/routine/.batch/

# Pre-write seed backups (definitions/apply.ts and any other seed-mutating step)
.local-backups/

# Env / secrets (Vite's default only ignores *.local)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
