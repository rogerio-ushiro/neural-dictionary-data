# npm 10.9.x arborist bug ("Cannot read properties of null (reading 'edgesOut')")
# hits while resolving vitest's optional peer set. Also applied when the app
# installs this package as a github: dep and runs `prepare` -> devDeps install.
legacy-peer-deps=true
